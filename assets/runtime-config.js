// Backend URL for production deployments (Vercel frontend -> Railway backend).
// Edit this after your first Railway deploy. Left as the placeholder, the
// playground/widget fall back to http://localhost:8000 for local dev.
window.NIMBUS_API_BASE = "https://REPLACE-ME.up.railway.app";
